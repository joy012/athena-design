import React from 'react';
import Header from '../Header/Header';
import Menu from '../Menu/Menu';

const Main = () => {
    return (
        <main>
            <Menu />
            <Header />
        </main>
    );
};

export default Main;
