import React from 'react';
import Header from '../Header/Header';
import Menu from '../Menu/Menu';

const Main = () => {
    return (
        <header>
            <Menu />
            <Header />
        </header>
    );
};

export default Main;
